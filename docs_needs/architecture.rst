Architecture
============

Mirrors ``docs/architecture.sdoc``'s ``ARCH-1``. The parent link is
expressed with sphinx-needs' ``:links:`` option instead of StrictDoc's
``RELATIONS: TYPE: Parent``.

.. spec:: Calculator module
   :id: ARCH-1
   :links: REQ-1, REQ-2

   A ``calculator`` Python module implements the arithmetic operations
   required by REQ-1 and REQ-2.
