SignalFilter Requirements
==========================

.. req:: Smooth noisy sensor signal
   :id: REQ-2

   The system shall smooth a noisy input stream using a moving-average
   filter over a configurable window size.
